// Initialize an array
let arr = [1, 2, 3, 4];

// Element to add
let newElement = 5;

// Add the new element to the end of the array without using push
arr[arr.length] = newElement;

// Output the updated array
console.log(arr);
